# RMA Customer App 📦

[![Flutter](https://img.shields.io/badge/Flutter-3.10.0-02569B?logo=flutter)](https://flutter.dev)
[![Clean Architecture](https://img.shields.io/badge/Architecture-Clean_Architecture-green)](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
[![BLoC State Management](https://img.shields.io/badge/State_Management-BLoC-blue)](https://bloclibrary.dev)

تطبيق **RMA Customer** هو الحل المتكامل لإدارة الشحنات والطرود، مصمم ليوفر للمستخدمين تجربة سلسة في تتبع شحناتهم، إدارة التخويلات، وحجز المواعيد.

---

## 🚀 المميزات الرئيسية (Features)

- **تتبع حي (Live Tracking):** متابعة موقع الطرود على الخريطة لحظة بلحظة.
- **إدارة الطرود:** إنشاء شحنات جديدة، عرض التفاصيل، ومعرفة التكلفة والوزن.
- **التخويلات (Authorizations):** إدارة صلاحيات استلام الطرود للضيوف والمفوضين.
- **المسارات (Routes):** استعراض المسارات المتاحة يومياً مع تفاصيل السائقين.
- **نظام المواعيد:** حجز مواعيد لاستلام أو تسليم الطرود من الفروع.
- **نظام التقييم:** تقييم الفروع والطرود لتحسين جودة الخدمة.
- **دخول الضيوف (Guest Access):** إمكانية تصفح الخدمات الأساسية دون الحاجة لتسجيل حساب.

---

## 🛠 التقنيات والمكتبات المستخدمة (Tech Stack & Libraries)

يعتمد التطبيق على مجموعة من المكتبات القوية لضمان الأداء العالي والسهولة في الصيانة:

### 🧩 إدارة الحالة والبيانات (State & Data)
- **[flutter_bloc](https://pub.dev/packages/flutter_bloc) & [hydrated_bloc](https://pub.dev/packages/hydrated_bloc):** لإدارة حالة التطبيق بشكل تنبؤي مع دعم الحفظ التلقائي للحالة (Persistence).
- **[equatable](https://pub.dev/packages/equatable):** لتسهيل مقارنة الكائنات وتحسين أداء الـ BLoC.
- **[dartz](https://pub.dev/packages/dartz):** تطبيق مفاهيم البرمجة الوظيفية (Functional Programming) مثل `Either` لمعالجة الأخطاء بشكل احترافي.

### 🌐 الشبكات والـ API (Networking)
- **[dio](https://pub.dev/packages/dio):** العميل الأساسي لطلبات HTTP مع دعم الـ Interceptors لإدارة التوكنات والأخطاء.
- **[retrofit](https://pub.dev/packages/retrofit):** لإنشاء طبقة الـ API بشكل تلقائي ونظيف (Type-safe).
- **[internet_connection_checker](https://pub.dev/packages/internet_connection_checker):** لمراقبة حالة الاتصال بالإنترنت لحظياً.

### 🏗 هيكلية المشروع وحقن التبعيات (Architecture & DI)
- **[get_it](https://pub.dev/packages/get_it) & [injectable](https://pub.dev/packages/injectable):** نظام قوي لحقن التبعيات لضمان سهولة الاختبار (Unit Testing) وفصل المهام.
- **[go_router](https://pub.dev/packages/go_router):** نظام تنقل متطور يدعم الـ Deep Linking والـ Declarative Routing.

### 📝 النماذج والتحقق (Forms & Validation)
- **[reactive_forms](https://pub.dev/packages/reactive_forms):** لإدارة النماذج المعقدة (مثل إنشاء الطرود) بطريقة برمجية سهلة وقوية التحقق.

### 🗺 الخرائط والوسائط (Maps & Media)
- **[flutter_map](https://pub.dev/packages/flutter_map) & [latlong2](https://pub.dev/packages/latlong2):** لعرض الخرائط التفاعلية وتتبع الشحنات دون الحاجة لمكتبات جوجل المكلفة.
- **[cached_network_image](https://pub.dev/packages/cached_network_image):** لتحميل الصور من الإنترنت مع ميزة التخزين المؤقت لتحسين السرعة.
- **[flutter_svg](https://pub.dev/packages/flutter_svg):** لعرض الأيقونات والرسومات المتجهة بجودة عالية.

### 🔐 التخزين والأمان (Storage & Security)
- **[flutter_secure_storage](https://pub.dev/packages/flutter_secure_storage):** لتخزين بيانات الحساسة (مثل توكن تسجيل الدخول) بشكل مشفر.
- **[shared_preferences](https://pub.dev/packages/shared_preferences):** لتخزين إعدادات التطبيق البسيطة.

---

## 📱 شرح أقسام التطبيق بالتفصيل (Application Sections)

### 🔐 1. نظام المصادقة والدخول الذكي (Auth & Guest System)

يعد هذا القسم بوابة المستخدم للتطبيق، وقد تم تصميمه ليكون مرناً وسهلاً:

#### **أ- رحلة المستخدم (User Journey):**
1. **شاشة الترحيب:** مخيرة بين تسجيل الدخول، إنشاء حساب، أو "الدخول كضيف".
2. **التحقق من البيانات:** يتم استخدام `reactive_forms` لضمان صحة البريد الإلكتروني وقوة كلمة المرور قبل إرسال الطلب.
3. **استعادة الحساب:** ميزة فريدة تتيح للمستخدم استلام رمز التحقق عبر **البريد الإلكتروني** أو مباشرة عبر **بوت التلغرام** الخاص بالشركة لسرعة الوصول.
4. **الجلسة المستمرة:** بمجرد الدخول، يتم حفظ التوكن في `flutter_secure_storage` ويتم تجديده تلقائياً لضمان عدم خروج المستخدم بشكل مفاجئ.

#### **ب- وضع الضيف (Guest Mode Deep Dive):**
- **الفلسفة:** صُمم هذا الوضع لجذب المستخدمين الجدد (Conversion). يمكن للضيف تتبع شحنة معينة عبر رقم التتبع فقط دون تسجيل.
- **القيود الذكية:** عند محاولة الضيف الوصول لميزات خاصة (مثل إنشاء طرد أو طلب تخويل)، يظهر له `GuestPromptBottomSheet` (مصمم بـ `shimmer` لإعطاء مظهر جذاب) يشرح له فوائد التسجيل.
- **التحويل:** يمكن للضيف في أي لحظة التحول لمستخدم كامل دون فقدان البيانات التي قام بتصفحها.

#### **ج- المكتبات المرتبطة بهذا القسم:**
- `reactive_forms`: لإدارة المدخلات والتحقق.
- `dio`: لإرسال طلبات تسجيل الدخول والاشتراك.
- `flutter_secure_storage`: لحماية بيانات الجلسة.
- `flutter_bloc`: لإدارة حالات الدخول (Authenticated, Unauthenticated, Loading, Error).

---

### 📦 2. إدارة الشحنات والطرود (Parcel Management)

هذا القسم هو القلب النابض للتطبيق، حيث يتم تنظيم كافة العمليات اللوجستية المعقدة بأسلوب بسيط للمستخدم:

#### **أ- رحلة المستخدم (User Journey):**
1. **الإنشاء الذكي (Smart Creation):** واجهة مخصصة لإضافة الطرود تطلب بيانات المستلم الأساسية (الاسم، الهاتف، العنوان). يتم اختيار المسار (Route) المناسب من قائمة المسارات المتاحة، مع حساب الوزن الفعلي.
2. **التتبع اللحظي (Real-time Tracking):** يوفر التطبيق تبويبات منفصلة للطرود **الصادرة (Outgoing)** و**الواردة (Incoming)**. يتم تحديث حالة الطرد تلقائياً عبر النظام.
3. **الشفافية المالية (Financial Transparency):** لكل طرد تفاصيل مالية واضحة تشمل تكلفة الشحن، والرسوم الإضافية إن وجدت، مع توضيح حالة الدفع (مدفوع/غير مدفوع).

#### **ب- التفاصيل التقنية (Technical Deep Dive):**
- **إدارة الحالات (State Machines):** يتم التحكم في دورة حياة الطرد عبر `ParcelStatus Enum` الذي يربط كل حالة (مثل `Picked up` أو `In Transit`) بلون وأيقونة ونص وصفي محدد في طبقة الـ Presentation، مما يضمن تناسق البيانات.
- **التواصل المباشر:** تم دمج ميزة الاتصال الهاتفي السريع بالمرسل أو المستلم باستخدام مكتبة `url_launcher` لتسهيل التنسيق اللوجستي.
- **تحسين الأداء:** استخدام `ListView.separated` مع `ParcelCard` مخصص لضمان سلاسة التمرير حتى مع وجود مئات الشحنات في السجل.

#### **ج- المكتبات المرتبطة:**
- `flutter_bloc`: لإدارة تدفق بيانات الطرود من الـ API إلى الواجهات.
- `equatable`: لضمان عدم إعادة بناء الواجهات (Rebuilds) إلا عند تغير بيانات الطرد فعلياً.

---

### 🛡 3. مركز التخويلات الأمنية (Security Authorizations)

نظام أمني مبتكر لحماية الشحنات وضمان وصولها للأشخاص المصرح لهم فقط:

#### **أ- رحلة المستخدم (User Journey):**
1. **إنشاء التخويل:** عندما يتعذر على المستلم الأصلي استلام الشحنة، يمكنه إنشاء "تخويل" لشخص آخر. يدعم النظام نوعين من المفوضين:
    - **مستخدم مسجل (Registered User):** عبر إدخال المعرف الخاص به (User ID).
    - **ضيف (Guest):** عبر إدخال بياناته الكاملة (الاسم، الرقم الوطني، الهاتف، المدينة).
2. **كود التحقق (Authorization Code):** يولد النظام كوداً فريداً لكل عملية تخويل. يجب على الشخص المفوض إبراز هذا الكود عند الاستلام.
3. **تتبع الحالة:** يمكن للمستخدم مراقبة حالة التخويل (Pending, Accepted, Used, Cancelled) مع إمكانية إلغاء التخويل في أي وقت قبل استخدامه.

#### **ب- التفاصيل التقنية (Technical Deep Dive):**
- **الأمان والخصوصية:** يتم تشفير أكواد التخويل ومعالجتها عبر طبقة أمان في الـ Backend، بينما يعرض التطبيق تفاصيل محدودة لضمان الخصوصية.
- **التحقق المزدوج:** يدعم النظام الربط بين `parcelId` و `authorizedUserId` لضمان عدم استخدام التخويل إلا للشحنة المحددة.
- **سجل الإلغاء:** عند إلغاء أي تخويل، يطلب النظام "سبب الإلغاء" (Cancellation Reason) ويتم أرشفته لأغراض التدقيق.

#### **ج- المكتبات المرتبطة:**
- `intl`: لإدارة تواريخ انتهاء صلاحية التخويلات (Expiry Dates).
- `dartz`: لمعالجة العمليات التي قد تفشل (مثل إنشاء تخويل لشحنة مستلمة بالفعل) باستخدام `Either<Failure, Success>`.

---

### 🗺 4. خريطة التتبع التفاعلية (Interactive Tracking Map)

تجربة بصرية متكاملة لمتابعة حركة الشحنات:

#### **أ- رحلة المستخدم:**
- يضغط المستخدم على "تتبع على الخريطة" ليفتح له نظام خرائط حي يظهر موقع السائق الحالي بالنسبة لموقع التسليم المتوقع.

#### **ب- التفاصيل التقنية:**
- الاعتماد على `flutter_map` (OpenStreetMap) لضمان الاستقلالية عن خدمات جوجل.
- استخدام `latlong2` لمعالجة الإحداثيات الجغرافية بدقة.

#### **ج- المكتبات المرتبطة:**
- `flutter_map`, `latlong2`.

---

### 🛣 5. المسارات والرحلات اليومية (Daily Routes)

تنظيم حركة النقل بين المدن السورية بوضوح:

#### **أ- رحلة المستخدم:**
- تصفح الرحلات المجدولة يومياً بين المحافظات، معرفة السائق المسؤول، وإمكانية الاتصال به مباشرة للتنسيق.

#### **ب- المكتبات المرتبطة:**
- `url_launcher`: لتنفيذ الاتصالات الهاتفية المباشرة من داخل التطبيق.

---

### 📅 6. نظام حجز المواعيد (Appointments System)

لتقليل الازدحام وتحسين كفاءة العمل في الفروع:

#### **أ- رحلة المستخدم:**
- اختيار الفرع، ثم تحديد اليوم والوقت المتاح. يحصل المستخدم على تذكرة رقمية (Digital Ticket) لموعده.

#### **ب- المكتبات المرتبطة:**
- `intl`: لتنسيق التواريخ والأوقات حسب المنطقة الزمنية.

---

### ⭐️ 7. نظام التقييم والملاحظات (Rating System)

حلقة الوصل لتحسين الجودة المستمر:

#### **أ- رحلة المستخدم:**
- بعد استلام كل طرد، يطلب التطبيق من المستخدم تقييم تجربته مع السائق، الفرع، وسرعة التوصيل.

---

## 🏗 هيكلية المشروع (Architecture)

نعتمد في هذا المشروع على **Clean Architecture** لضمان فصل المهام (Separation of Concerns):

1. **Core:** يحتوي على الثيمات، الألوان، الإعدادات العامة، والتعامل مع الشبكة.
2. **Features:** ينقسم كل قسم (مثل Auth, Parcels, Routes) إلى:
   - **Data:** المستودعات (Repositories) ومصادر البيانات (Data Sources).
   - **Domain:** الكيانات (Entities) وحالات الاستخدام (Use Cases).
   - **Presentation:** واجهات المستخدم (UI) وإدارة الحالة (BLoC).

---

## 📦 كيفية التشغيل (Getting Started)

1. تأكد من تثبيت Flutter (SDK 3.10.0+).
2. قم بتحميل المستودع:
   ```bash
   git clone https://github.com/assem2023-habib/rma-mobile.git
   ```
3. تثبيت المكتبات:
   ```bash
   flutter pub get
   ```
4. تشغيل التطبيق:
   ```bash
   flutter run
   ```

---

## 📄 توثيق الـ API

يمكنك العثور على التوثيق الكامل للـ Endpoints المستخدمة في ملف [FLUTTER_API_DOCUMENTATION.md](./FLUTTER_API_DOCUMENTATION.md).

---

## 🤝 المساهمة

نرحب بمساهماتكم لتطوير التطبيق! يرجى فتح Issue أو تقديم Pull Request.

---

### 📝 ملاحظات المطور
تم بناء هذا التطبيق مع مراعاة أفضل ممارسات الأداء (Performance) وتجربة المستخدم (UX)، مع دعم كامل للغة العربية.
